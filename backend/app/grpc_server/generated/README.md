# Generated gRPC stubs

This folder is populated by `scripts/generate_proto.sh` (run automatically
during the Docker image build). Do not edit files here by hand — re-run the
script instead, which regenerates from `proto/board_stream.proto`.

Locally (outside Docker), run:

```bash
pip install -e ".[dev]"
./scripts/generate_proto.sh
```
